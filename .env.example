# Common variables for all attestation types
PORT=
API_KEYS=

# TeeAvailabilityCheck
VERIFIER_TYPE=TeeAvailabilityCheck
SOURCE_ID=TEE
RELAY_CONTRACT_ADDRESS=
RPC_URL=  # Flare RPC
TEE_AUDIENCE= # Expected aud claim on the workload's attestation token. tee-node currently hardcodes "https://sts.google.com". Required unless DISABLE_ATTESTATION_CHECK_E2E=true.
TEE_ALLOWED_IMAGE_IDS= # Comma-separated 32-byte hex hashes (with or without 0x prefix) of accepted workload container image_ids (sha256 of submods.container.image_id). Required unless DISABLE_ATTESTATION_CHECK_E2E=true.
ALLOW_TEE_DEBUG=false # set true to ALSO accept debug-mode TEEs (in addition to production); never enable in production
DISABLE_ATTESTATION_CHECK_E2E=false # Disables attestation checks for E2E tests. Only for test deployments.
ALLOW_PRIVATE_NETWORKS=false # Allows private/loopback IPs while still blocking dangerous IPs and preserving DNS pinning.

# PMWMultisigAccountConfigured
VERIFIER_TYPE=PMWMultisigAccountConfigured
SOURCE_ID=testXRP
RPC_URL= # Source RPC URL (i.e., testXRP, XRP)

# PMWPaymentStatus
VERIFIER_TYPE=PMWPaymentStatus
SOURCE_ID=testXRP
CCHAIN_DATABASE_URL= # Flare C-Chain indexer database
SOURCE_DATABASE_URL= # Source indexer database (i.e., testXRP, XRP)
FLARE_TEE_MANAGER_CONTRACT_ADDRESS= # Contract that emits TeeInstructionsSent events

# PMWFeeProof
VERIFIER_TYPE=PMWFeeProof
SOURCE_ID=testXRP
CCHAIN_DATABASE_URL= # Flare C-Chain indexer database
SOURCE_DATABASE_URL= # Source indexer database (i.e., testXRP, XRP)
FLARE_TEE_MANAGER_CONTRACT_ADDRESS= # Contract that emits TeeInstructionsSent events